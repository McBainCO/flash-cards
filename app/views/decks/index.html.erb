<%= link_to "New Deck", new_deck_path %>


<% @decks.each do |deck| %>

   <%= link_to deck.name, deck_path(deck.id) %>

   <%= link_to "Edit", edit_deck_path(deck.id) %>

   <%= link_to "Delete", deck_path(deck.id), method: :delete, confirm: "Are you sure?" %>

<% end %>